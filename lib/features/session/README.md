# Session Feature (`lib/features/session/`)

## Responsibility

Manages cryptographic session authentication, secure key storage, token generation, and secure handshake sessions with smart lock hardware.
