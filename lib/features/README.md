# Features Module (`lib/features/`)

This directory houses modular feature domains for the NFC Smart Lock application:

- `lock/`: Smart lock status, device metadata, lock/unlock operations, and lock state management.
- `nfc/`: Low-level NFC hardware interaction, tag discovery, and APDU/NDEF communication.
- `session/`: Authenticated lock sessions, secure key exchange, and session security token management.
