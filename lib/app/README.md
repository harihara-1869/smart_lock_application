# App Module (`lib/app/`)

This directory contains application-level scaffolding and global configurations:

- `app.dart`: Root widget (`App`) configuring `MaterialApp` with Material 3.
- `theme.dart`: Application theme definitions using Material 3 (`useMaterial3: true`).
